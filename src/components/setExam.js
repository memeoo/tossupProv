import React from 'react';
import './BigText.css';

const setTexam = ({children}) => {
    return (
        <div className="big-text">
            {children}
        </div>
    );
};

export default setTexam;